{{#> layout }}
  <h2>Tokens</h2>
  {{#if tokens}}
    <ul>
      {{#each tokens}}
        <li>{{userid}}: <a href="/vks/v1/verify/{{token}}"><tt>{{token}}</tt></a></li>
      {{/each}}
    </ul>
  {{else}}
    <em>No new user IDs</em>
  {{/if}}
{{/layout}}
