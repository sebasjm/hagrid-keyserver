{{#> layout }}
  <div class="row">
    <p class="found-key">
    We found an entry for <em>{{ query }}</em>.
    </p>

    <p>You can get it with GnuPG using the following snippet:</p>
    <pre><code>gpg --fetch-keys <a href="https://{{ domain }}/vks/by-fingerprint/{{ fpr }}">https://{{ domain }}/vks/by-fingerprint/{{ fpr }}</a></code></pre>
  </div>
{{/layout}}
